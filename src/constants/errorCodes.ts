export enum ErrorCodes {
  // General Errors
  INTERNAL_SERVER_ERROR = 1000,
  BAD_REQUEST = 1001,
  NOT_FOUND = 1002,
  UNAUTHORIZED = 1003,
  FORBIDDEN = 1004,
  CONFLICT = 1005,
  UNPROCESSABLE_ENTITY = 1006,
  SERVICE_UNAVAILABLE = 1007,

  // User-related Errors
  USER_NOT_FOUND = 2000,
  USER_ALREADY_EXISTS = 2001,
  INVALID_USER_INPUT = 2002,
  USER_UNAUTHORIZED = 2003,

  // Authentication & Authorization Errors
  AUTHENTICATION_FAILED = 3000,
  TOKEN_EXPIRED = 3001,
  TOKEN_INVALID = 3002,
  PERMISSION_DENIED = 3003,

  // Database Errors
  DATABASE_ERROR = 4000,
  DATABASE_CONNECTION_FAILED = 4001,
  DUPLICATE_KEY_ERROR = 4002,
  RECORD_NOT_FOUND = 4003,

  // Validation Errors
  VALIDATION_ERROR = 5000,
  INVALID_PARAMETERS = 5001,
  MISSING_PARAMETERS = 5002,

  // External Service Errors
  EXTERNAL_SERVICE_ERROR = 6000,
  EXTERNAL_SERVICE_TIMEOUT = 6001,

  // File Errors
  FILE_NOT_FOUND = 7000,
  FILE_UPLOAD_ERROR = 7001,
  FILE_FORMAT_UNSUPPORTED = 7002,

  // Rate Limiting Errors
  TOO_MANY_REQUESTS = 8000,

  // Payment Errors
  PAYMENT_FAILED = 9000,
  PAYMENT_DECLINED = 9001,

  // Custom Application-specific Errors
  CUSTOM_ERROR = 10000,
}
